---
title:
description:
outcome:
date:
serves:
---

_Copy this directory to `outputs/YYYY-MM-DD-<slug>/` and fill it in. `outcome:`
is the field the archive exists for: what this run actually produced, in
numbers. "none" is a valid outcome with a reason after it._

## What ran

_The play, agent, or manual pass. Link the code in `infra/` and the context it
read, so the entry can be re-read later without reconstructing the setup._

- Play:
- Trigger:
- Context used:

## Headline result

_Counts, rates, and cost. The numbers someone would quote in a review._

-

## Where the full data lives

_Entries stay small. Large raw data goes to workspace storage and is linked from
here, never committed._

-

## What we learned (candidate for context/)

_The durable conclusion, if there is one. A claim reaches `context/` on its
second independent occurrence, so a single observation stays here as evidence
and the Friday review promotes it if it repeats._
