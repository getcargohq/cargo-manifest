# Apps

Hosted Vite apps on *.cargo.app (`defineApp`): rep cockpits, pipeline
dashboards, internal surfaces. Each app is a Vite sub-project that
`defineApp({ path })` points at; Cargo Hosting builds and promotes it on
deploy. Empty until you add one.
