# Segments

Saved views over models (`defineSegment`): ICP tiers, stale records,
unrouted accounts. Empty until you add one; run `npx cargo-ai cdk types` first
so the segment filter config type-checks against your workspace schema.
